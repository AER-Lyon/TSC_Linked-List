/*
** EPITECH PROJECT, 2023
** Linked-List
** File description:
** creation
*/

#include "include/my.h"

void add_element_at_end(linked_list_t *elem, char *data)
{
    linked_list_t *head = elem; // Save a pointer to the head of the list
    linked_list_t *new_elem = malloc(/* TODO: allocate memory for the new element*/ 1);
    // TODO: add the new element at the end of the list
}

